[package]
name = "webcut"
version = "0.6.4"
description = "webCut desktop"
authors = ["feeday"]
edition = "2021"

[lib]
name = "webcut_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rfd = "0.15"
